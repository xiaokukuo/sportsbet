<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sport.bet.core.mapper.SportModuleGameMapper">

	<resultMap id="SportModuleGameMap" type="com.sport.bet.bean.model.SportModuleGame">
		<id column="id" property="id"/>
		<id column="resourceId" property="resource_id" />
		<id column="sportGroupId" property="sport_group_id" />
		<id column="teamName1" property="team_name1" />
		<id column="teamName2" property="team_name2" />
		<id column="deailPd" property="deail_pd" />
		<id column="gameTime" property="game_time" />
		<id column="gameType" property="game_type" />
	</resultMap>

	<insert id="insert" keyProperty="sportGame.id" useGeneratedKeys="true">
		insert into sport_module_${tbName}
		(resource_id, sport_group_id, team_name1, team_name2, deail_pd, game_time,game_type, create_time)
		values
		(#{sportGame.resourceId}, #{sportGame.sprotGroupId}, #{sportGame.teamName1}, #{sportGame.teamName2},
		 #{sportGame.deailPd}, #{sportGame.gameTime}, #{sportGame.gameType}, now())
	</insert>
	
	
	<insert id="insertByBatch">
        insert into sport_module_${tbName}
		(resource_id, sport_group_id, team_name1, team_name2, deail_pd, game_time,game_type, create_time)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.resourceId}, #{item.sprotGroupId}, #{item.teamName1}, #{item.teamName2},
		 		#{item.deailPd}, #{item.gameTime}, #{item.gameType}, now())
        </foreach>
    </insert>


</mapper>